<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Test_Planigo" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Test_Planigo" parent="Base.Theme.Test_Planigo" />

    <!-- Splash Screen Theme -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Couleur de fond du splash screen -->
        <item name="windowSplashScreenBackground">@color/light_gray_background</item>
        <!-- Icône (votre logo) -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/planigologo</item>
        <!-- Durée de l'icône à l'écran (optionnel) -->
        <!-- <item name="windowSplashScreenAnimationDuration">1000</item> -->
        <!-- Thème à appliquer APRÈS le splash screen -->
        <item name="postSplashScreenTheme">@style/Theme.Test_Planigo</item>
    </style>

    <!-- Style pour les titres des jours du Planner -->
    <style name="DayTitleStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/principal_color</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_marginTop">16dp</item>
        <item name="android:layout_marginBottom">8dp</item>
    </style>

    <style name="EditProfileRowStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:layout_marginBottom">12dp</item>
        <item name="android:background">@drawable/white_rounded_background</item>
        <item name="android:padding">16dp</item>
    </style>

    <style name="EditProfileIconStyle">
        <item name="android:layout_width">24dp</item>
        <item name="android:layout_height">24dp</item>
        <item name="android:layout_marginEnd">12dp</item>
        <item name="tint">@color/secondary_color</item>
    </style>

    <style name="EditProfileLabelStyle">
        <item name="android:layout_width">80dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/hint_text_color</item>
        <item name="android:layout_marginEnd">8dp</item>
    </style>

    <style name="EditProfileValueStyle">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/black_text</item>
        <item name="android:gravity">start</item>
        <item name="android:layout_marginEnd">8dp</item>
    </style>

    <style name="EditProfilePencilStyle">
        <item name="android:layout_width">24dp</item>
        <item name="android:layout_height">24dp</item>
        <item name="android:src">@drawable/pencil</item>
        <item name="android:contentDescription">@string/edit_profile</item>
        <item name="tint">@color/principal_color</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:background">?android:attr/selectableItemBackgroundBorderless</item>
    </style>


</resources>